# Storage

Document storage helpers live here.

`document-storage.ts` defines the private `project-documents` bucket and object path convention for future Supabase Storage upload/download flows. Sprint 3 does not implement production upload; callers should keep using external URLs until signed upload and signed download actions are added.
