# Vercel Notes

Recommended Vercel settings:

- Framework preset: Next.js.
- Build command: `npm run build`.
- Install command: `npm install`.
- Output directory: Next.js default.

Use Vercel preview deployments for QA before production.
